/*global define*/

define([
    'underscore',
    'backbone'
], function (_, Backbone) {
    'use strict';

    var TimeModel = Backbone.Model.extend({
        defaults: {
        }
    });

    return TimeModel;
});
